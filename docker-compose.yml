version: '3.8'

services:
  backend:
    build:
      context: ./backend
    ports:
      - "5000:5000"
    environment:
      - PORT=5000
      - MONGO_URI=mongodb+srv://shubhams45665_db_user:VbP5BnkRBOkFeOzt@cluster0.xnt4tle.mongodb.net/smartleads?appName=Cluster0
      - JWT_SECRET=supersecretjwtkey
      - JWT_EXPIRES_IN=7d
      - CLIENT_URL=http://localhost:3000
    networks:
      - mern-network

  frontend:
    build:
      context: ./frontend
    ports:
      - "3000:3000"
    environment:
      - VITE_API_URL=http://localhost:5000/api
    depends_on:
      - backend
    networks:
      - mern-network

networks:
  mern-network:
    driver: bridge
